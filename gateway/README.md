# gateway/ (later step)

Attestation **verifier** that sits in front of the SIEM. Checks each
envelope's signature, freshness, replay nonce, and sequence before forwarding
only trusted telemetry. Not built yet.
