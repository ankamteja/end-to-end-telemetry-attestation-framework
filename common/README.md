# common/ (later step)

Shared code for sources and gateway: the signed attestation **envelope**
format and **Ed25519** signing/verification helpers. Empty for now -- built
after the network (step 1) is up.
