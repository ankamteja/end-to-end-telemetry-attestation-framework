# agent/ (later step)

Attestation **sidecar** that runs on each telemetry source. Wraps every event
in a signed envelope (integrity + authenticity + freshness) and ships it to
the gateway. Not built yet.
